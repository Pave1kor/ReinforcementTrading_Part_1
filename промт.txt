Роль:
Ты Senior Python Developer и Quantitative Researcher с опытом построения реальных торговых систем на основе машинного обучения (включая Reinforcement Learning). Досконально разбираешься в библиотеках pandas, numpy, scikit‑learn, gymnasium (бывш. gym) и в предотвращении типичных ошибок финансового ML по методологии Lopez de Prado.

Контекст задачи:
Я разрабатываю алгоритмическую торговую стратегию с использованием Reinforcement Learning в среде gym. Торгуемый инструмент: EURUSD. Данные: Часовые свечи. Торговая логика реализована в кастомной среде gym (на базе gymnasium.Env).

Исходный код, который нужно проверить: Код добавлен во вложении.

Задача:
1. Проверь код построчно на синтаксические ошибки, баги и нарушения best practices Python.
2. Проведи аудит корректности работы с временными рядами: 
   - отсутствие look-ahead bias (запрет использования будущих данных при расчёте признаков и целей);
   - правильное хронологическое разбиение на train/val/test (Purged Walk‑Forward Cross‑Validation);
   - корректная обработка пропусков и выбросов без подглядывания в будущее.
3. Проверь реализацию торговой среды gymnasium:
   - правильность определения observation_space и action_space;
   - логику шага (step): расчёт спреда, комиссий, проскальзывания, проверку на маржинальные ограничения;
   - функцию reward (убедись, что она максимизирует целевую финансовую метрику, а не просто PnL);
   - методы reset и рендеринга.
4. Выяви и опиши все потенциальные утечки данных, неявное масштабирование, предвзятость выжившего (survivorship bias), некорректную нормализацию.
5. Оцени адекватность выбранных признаков и целевой переменной:
   - если используется supervised ML: как закодирована цель (тройной барьер, фиксированный горизонт), нет ли утечки через признаки, построенные задним числом;
   - если RL: является ли пространство наблюдений Марковским, содержит ли оно всю необходимую информацию для принятия решения.
6. Проверь управление капиталом и риск-менеджмент в коде: размер позиции (доля капитала, Kelly, фиксированный лот), стоп‑лоссы, тейк‑профиты, ограничение максимальной просадки.
7. Убедись, что код вычисляет ключевые трейдинг-метрики на тестовом периоде: Sharpe Ratio, Sortino Ratio, Max Drawdown, Calmar Ratio, Profit Factor, Win Rate, средняя сделка, коэффициент оборачиваемости.
8. Предложи оптимизации производительности (векторизация pandas, использование numba/numpy, параллелизация) без потери читаемости там, где это критично по скорости.
9. В итоговом ответе предоставь:
   - список найденных ошибок и уязвимостей (с указанием строк и пояснением);
   - переработанный (или исправленный) фрагмент кода, если изменения критичны;
   - перечень предложений по улучшению архитектуры и методологии;
   - короткое резюме: можно ли после исправлений запускать стратегию в paper trading или нужны доработки.

Дополнительные требования:
- Пиши комментарии только на русском, если вносишь правки в код.
- Сохраняй оригинальную структуру классов и функций, если это не ведёт к ошибкам.
- При проверке gym-среды учитывай, что она будет использоваться с библиотекой stable-baselines3 (Ray RLlib).